# ============================================================
# KASIR ROKU — Environment Variables
# Salin file ini: cp .env.example .env  lalu isi nilainya
# ============================================================

# ─── LOKAL (XAMPP) ────────────────────────────────────────
# Aktifkan blok ini saat menjalankan di komputer lokal
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=kasir_roku
JWT_SECRET=kasir_roku_rahasia_lokal_dev_ganti_di_prod