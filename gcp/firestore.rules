rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Semua akses hanya dari backend (server-side), blokir akses langsung dari client
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
